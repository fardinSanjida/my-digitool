import React from 'react';

const Cart = ({carts}) => {
    console.log(carts);
    return (
        <div >
          Cart is empty. Please add some products to your cart.
        </div>
    );
};

export default Cart;
